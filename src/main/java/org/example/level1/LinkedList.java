package org.example.level1;

public class LinkedList {
    Node reverse(Node head){
        // enter your code
        return null;
    }

    interface Node{
        Node getNext();

        void setNext(Node next);
    }

    class NodeLL implements Node {

        @Override
        public Node getNext() {
            return null;
        }

        @Override
        public void setNext(Node next) {

        }
    }
}
